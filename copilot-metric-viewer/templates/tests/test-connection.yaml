apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "copilot-metric-viewer.fullname" . }}-test-connection"
  labels:
    {{- include "copilot-metric-viewer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "copilot-metric-viewer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
